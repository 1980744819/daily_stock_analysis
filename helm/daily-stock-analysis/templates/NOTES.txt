Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
  $ helm get all {{ .Release.Name }} -n {{ .Release.Namespace }}

**IMPORTANT: This chart requires an external Secret and will fail fast if it is missing:**

  $ kubectl apply -f /path/to/secret-external.yaml -n {{ .Release.Namespace }}

  Secret name expected by this chart: {{ .Values.secret.name }}

**To uninstall:**

  $ helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}

**To access the API service:**

  NodePort: {{ .Values.service.nodePort }}
  URL: http://<node-ip>:{{ .Values.service.nodePort }}/api/health
